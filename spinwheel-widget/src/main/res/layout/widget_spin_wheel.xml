<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/widget_padding"
    android:background="@drawable/bg_wheel">

    <!-- Loading State -->
    <ProgressBar
        android:id="@+id/progress_loading"
        android:layout_width="@dimen/loading_size"
        android:layout_height="@dimen/loading_size"
        android:layout_gravity="center"
        android:visibility="visible"
        android:indeterminateTint="@android:color/white" />

    <!-- Error State -->
    <TextView
        android:id="@+id/tv_error"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:textColor="#FF6B6B"
        android:textSize="@dimen/error_text_size"
        android:padding="16dp"
        android:visibility="gone"
        android:text="@string/error_unknown" />

    <!-- Composed Wheel Image (background + wheel + frame + button) -->
    <ImageView
        android:id="@+id/iv_wheel_composed"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:visibility="gone"
        android:contentDescription="@string/wheel_description" />

    <!-- Invisible Spin Button Overlay for Click Detection -->
    <View
        android:id="@+id/btn_spin"
        android:layout_width="@dimen/spin_button_size"
        android:layout_height="@dimen/spin_button_size"
        android:layout_gravity="center"
        android:background="@android:color/transparent"
        android:contentDescription="@string/spin_button_description" />

</FrameLayout>
